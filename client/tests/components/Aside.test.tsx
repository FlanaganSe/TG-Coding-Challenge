// Skipping this for now until I can figure out zustad setup...

describe("Aside", () => {
  it("should not render a select when there are no locations", () => {
    expect(true).toBe(true);
  });
});
